#!/usr/bin/env bash
# Run Splunk AppInspect against the packaged app (Developer Tools bonus evidence).
# INTENT (phase-8): package via SLIM, then run cloud-tagged AppInspect checks.
# Stub — wire to `slim package` + `splunk-appinspect inspect --mode test --included-tags cloud`.
set -euo pipefail
echo "TODO(phase-8): slim package spelunk_app + splunk-appinspect inspect --included-tags cloud"

{
  "name": "@spelunk/ui-contract",
  "version": "0.0.0",
  "private": true,
  "description": "Shared step-event types — hand-synced to spelunk_core/events.py. The generative-UI registry key.",
  "main": "src/events.ts",
  "types": "src/events.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

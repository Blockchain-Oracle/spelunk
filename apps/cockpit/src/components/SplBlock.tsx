/** SplBlock — renders a "spl" StepEvent (generated SPL) with a Run button.
 *  Run executes via SplunkJS SearchManager (same-origin, no CSP issue).
 *  INTENT (phase-4): code block + onRun. Stub. */
import * as React from "react";
export function SplBlock(): JSX.Element { return <div className="spelunk-spl-block" />; }

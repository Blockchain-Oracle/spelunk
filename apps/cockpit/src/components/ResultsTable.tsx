/** ResultsTable — renders a "results" StepEvent (search results rows).
 *  INTENT (phase-4): sortable table over @splunk/react-ui. Stub. */
import * as React from "react";
export function ResultsTable(): JSX.Element { return <div className="spelunk-results-table" />; }

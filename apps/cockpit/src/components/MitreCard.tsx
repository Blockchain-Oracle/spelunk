/** MitreCard — renders a "classification" StepEvent (Foundation-Sec -> MITRE technique).
 *  Shows technique id/name, tactic, confidence. INTENT (phase-4): real card. Stub. */
import * as React from "react";
export function MitreCard(): JSX.Element { return <div className="spelunk-mitre-card" />; }

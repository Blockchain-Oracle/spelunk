# tooling

Build + packaging helpers.

**INTENT (phase-8):**
- `package_app.sh` — vendor `packages/spelunk_core` into `apps/splunk_app/spelunk_app/bin/`, build the cockpit bundle, then `slim package` the app to a `.tgz`.
- AppInspect wrapper (also at `apps/splunk_app/spelunk_app/scripts/run_appinspect.sh`).

Stub placeholder; scripts not written yet.

#
# Expose the Spelunk REST endpoint through the Splunk Web proxy so the cockpit
# (browser, same-origin) can reach it at /splunkd/__raw/services/spelunk/chat.
#
# SPIKE (plan Phase 0): confirm the exact expose stanza + whether chunked/SSE
# bytes flush through this proxy or buffer (decides streaming vs polling).
#
[expose:spelunk_chat]
pattern = spelunk/chat
methods = GET,POST
